apiVersion: v1
kind: ServiceAccount
metadata:
  name: myapp-reloader-sa

---
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: myapp-reloader-role
rules:
  - apiGroups: ["apps"]
    resources: ["deployments"]
    verbs: ["get", "patch"]
  - apiGroups: [""]
    resources: ["pods","configmaps","secrets"]
    verbs: ["get","list","watch"]

---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: myapp-reloader-rb
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: myapp-reloader-role
subjects:
  - kind: ServiceAccount
    name: myapp-reloader-sa
    namespace: default
